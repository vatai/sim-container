benchmark:
  - precompiled.source:
      BIN: XSBench-peachy
      RUN_OPTIONS: "-t ${NCORES} -s small -g 10 -l 10"
sim_params:
  # - gem5:
  #     DOCKER_IMAGE: "riken/gem5"
  #     CPU_TYPE: "HPI"
  #     SIMULATOR: "gem5"
  #     CACHELINE: "128"
  #     CC: "aarch64-linux-gnu-gcc -static -O3"
  #     CXX: "aarch64-linux-gnu-g++ -static -O3"
  - riken_simulator:
      # JUST TO MAKE IT WORK
      CACHELINE: "256"
      NCORES: 8
