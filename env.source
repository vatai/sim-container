#!/bin/bash

# `BENCHMARK` specifies the file with the compilation parameters.
BENCHMARK=${BENCHMARK:-polybench.source}

HOST_BINDIR="$(pwd)/bin"
SIMDIR=${HOME}/riken_simulator
OUTDIR=${HOME}/m5out
SRCDIR=/tmp/src
BINDIR=/tmp/bin

source ${BENCHMARK}
source sim_param.source
