#!/bin/bash

CACHE_SIZE=${CACHE_SIZE:-1024MB}

HOST_OUTDIR=$(pwd)/${CACHE_SIZE}l2size
mkdir -p ${HOST_OUTDIR}
chmod o+rX ${HOST_OUTDIR}

SIM_PARAMS="--cpu-type=O3_ARM_PostK_3 --caches \
       --l2cache \
       --l2_size=${CACHE_SIZE}"