#!/bin/bash

CACHE_SIZE=${CACHE_SIZE:-8MB}
CACHELINE=${CACHELINE:-256}

HOST_OUTDIR=$(pwd)/m5out/${OUTPUT_PREFIX}/${CACHE_SIZE}l2size-${CACHELINE}cacheline

SIM_PARAMS="--cpu-type=O3_ARM_PostK_3 --caches \
       --l2cache \
       --l2_size=${CACHE_SIZE} \
       --cacheline_size=${CACHELINE}"
